<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="@dimen/margin_padding_sixteen">

    <ImageView
        android:id="@+id/iv_club_logo"
        android:layout_width="@dimen/margin_padding_sixty"
        android:layout_height="@dimen/margin_padding_sixty"
        android:src="@mipmap/ic_launcher"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:ignore="ContentDescription" />

    <TextView
        android:id="@+id/tv_club_name"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="@dimen/margin_padding_twenty"
        android:lines="1"
        android:text="@string/string_club_name"
        android:textSize="8pt"
        app:layout_constraintBottom_toBottomOf="@id/iv_club_logo"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintStart_toEndOf="@id/iv_club_logo"
        app:layout_constraintTop_toTopOf="@id/iv_club_logo" />

</androidx.constraintlayout.widget.ConstraintLayout>